#include<bits/stdc++.h>
using namespace std;
int dp(int i,int less,int last,int have0){
  if(f[i][less][last][have0]==-1){
    if(i==0)  f[i][less][last][have0]=1;
    else{
      f[i][less][last][have0]=0;
      int end=less ? 9 :a[i];
      for(int j=i;)
    }
  }
}
int main(){

  return 0;
}
